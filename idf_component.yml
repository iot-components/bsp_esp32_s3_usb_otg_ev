version: "1.0.0" # Component version, required only for components pushed to the service
targets: # List of supported targets (optional, if missing all targets are considered to be supported)
  - esp32s2
  - esp32s3
description: BSP bsp_esp32_s3_usb_otg_ev # Description (optional)
url: https://github.com/iot-components/bsp_esp32_s3_usb_otg_ev # Original repository (optional)
dependencies:
  # Required IDF version
  idf:
    version: ">=4.2"
  bus:
    git: https://github.com/iot-components/bus.git
    public: true
  button:
    git: https://github.com/iot-components/button.git
    public: true